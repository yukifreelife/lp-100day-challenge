interface:
  display_name: "LP Automation"
  short_description: "Compatibility alias for the older LP skill"
  default_prompt: "Use the current lp-100 LP automation workflow, even if the request uses the older lp-automation name."
